import React, { type JSX } from "react";

import AuthForm from "../components/AuthForm";

const AuthenticationPage: React.FC = (): JSX.Element => {
  return <AuthForm />;
};

export default AuthenticationPage;
